@model IEnumerable<ControlePagamentoEntidades.Models.ProcessoModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProcessoNumero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProcessoValorTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProcessoSituacao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProcessoNumero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProcessoValorTotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProcessoSituacao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProcessoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProcessoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProcessoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
